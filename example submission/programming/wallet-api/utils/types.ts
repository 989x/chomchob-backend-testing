export enum UserRole {
  ADMIN = "admin",
  GENERAL = "general",
  TRIAL = "trial",
  SUSPENDED = "suspended",
}

export enum UserStatus {
  PENDING = "pending",
  ACTIVE = "active",
  SUSPENDED = "suspended",
}
